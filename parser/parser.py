class Parser:
    """
    Anaalyze the list of tokens, match it against the language's syntax rules, and return a valid parse tree.
    """
    tokens = []

    def __init__(self, tokens):
        self.tokens = tokens


